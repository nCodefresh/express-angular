version: '1.0'
steps:
  build_step:
    type: build
    image-name: ncodefresh/ncodefresh
    build_arguments:
      - arg=argument

#  push-to-dockerhub:
#    type: push
#    candidate: ${{build_step}}
#    registry: ecr
#    tags: [ ${{CF_BRANCH}}, qwerty ]