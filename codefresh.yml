version: '1.0'
steps:

  test_image:
    image: ncodefresh/express-angular:test
    commands:
      - echo test

  push-to-dockerhub1:
    type: push
    candidate: ncodefresh/express-angular
    registry: dockerhub
    tag: improve-tag