version: '1.0'

steps:

  auto-gen-private-img:
    image: codefreshio/yaml-node-1
    commands:
        - echo private img
    fail-fast: false

  auto-gen-public-img:
    image: codefreshio/git-image
    commands:
        - echo public img
