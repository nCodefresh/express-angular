version: '1.0'
steps:

#  generate-tag:
#    image: node:latest
#    working-directory: ${{main_clone}}
#    commands:
#      - ls -a
#      - pwd
#      - npm install

  build_project:
    type: build
    description: codefresh example
    working_directory: ${{main_clone}}
    image-name: codefreshio/express-angular-test
    dockerfile: Dockerfile
    tag: codefresh

  push-to-dockerhub:
    type: push
    candidate: ${{build_project}}
    tag: latest
    credentials:
      username: ${{user}}
      password: ${{pass}}
