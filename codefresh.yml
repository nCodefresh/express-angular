version: '1.0'
steps:

  test_image:
    image: ncodefresh/express-angular:test
    commands:
      - echo test

  push-to-dockerhub1:
    type: push
    candidate: ${{test_image}}
    registry: dockerhub
    tag: improve-tag